import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  chartContainer: {
    flexDirection: "row",
    width: "100%",
    paddingVertical: 16,
    marginTop: 32,
    marginBottom: 16
  }
})
